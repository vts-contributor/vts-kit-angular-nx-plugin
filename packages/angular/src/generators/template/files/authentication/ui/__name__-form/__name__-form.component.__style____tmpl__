.authentication-form {
  display: flex;
  flex-direction: column;
  width: 100%;
  height: 100%;

  &-title {
    display: flex;

    .action {
      margin-left: auto;
      .vi,
      .en {
        display: flex;
        height: 36px;
        align-items: center;
        cursor: pointer;

        svg {
          line-height: 1;
          padding: 1px;
        }

        &:hover, &.active {
          > svg {
            border-radius: 50%;
            border: 1px solid #ee0033;
            background: #ffd8e0;
          }
        }
      }
    }
  }

  & &-content {
    margin-top: 16px;
  }

  &.content-margin &-content {
    margin-top: calc(24px + 48px);
  }

  &-footer {
    margin-top: auto;
    color: #73777a;
    text-align: center;
    padding-top: 4px;
  }
}
