.comment-card {
  box-shadow: 0px 4px 30px 4px rgba(0, 0, 0, 0.1);
  border-radius: 16px;
  height: 100%;
  padding: 24px;
  position: relative;
  background-color: var(--white-0);

  &-header {
    transform: translateX(calc(-24px - 56px / 2));

    &-avatar > img {
      height: 56px;
      width: 56px;
    }
  }

  &-icon {
    position: absolute;
    right: 0;
    top: 0;
    font-size: 56px;
    transform: translate(50%, -50%);
  }
}
